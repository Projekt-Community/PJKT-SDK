using UnityEngine;

//plan is to sign in somewhere, get the communities from the server, and then select one to work with
//then when they call upload we take the community id and texture and send it to senpai's stuff

internal static class PJKTImageUploader
{
    public static void UploadImage(Texture2D image)
    {
        //send texture and id to senpai
    }
}
